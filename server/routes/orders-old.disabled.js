// DISABLED: Legacy orders-old.js — moved here to prevent lint/parse errors
// This placeholder keeps the route import resolution safe while preserving historical code in
// version control. The original legacy implementation is preserved in the git history.

import express from 'express';

const router = express.Router();

// Legacy route placeholder — exports an empty router to ensure imports don't fail.
export default router;
